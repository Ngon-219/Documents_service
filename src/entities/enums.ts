export enum RoleEnum {
  ADMIN = 'admin',
  MANAGER = 'manager',
  TEACHER = 'teacher',
  STUDENT = 'student',
}

export enum UserStatus {
  PENDING = 'pending',
  SYNC = 'sync',
  FAILED = 'failed',
}

export enum RequestStatus {
  PENDING = 'pending',
  SCHEDULED = 'scheduled',
  REJECTED = 'rejected',
}


